[project]
name = "backend"
version = "0.1.0"
description = "Backend for Muscle Heatmap Workout Tracker"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "django>=5.0",
    "django-cors-headers>=4.9.0",
    "django-oidc-provider>=0.9.0",
    "djangorestframework>=3.18.1",
]

[tool.uv.workspace]
