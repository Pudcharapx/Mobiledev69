name: project
description: "Muscle Heatmap Workout Tracker"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.8.0+1
  openid_client: ^0.4.10+1
  flutter_secure_storage: ^9.2.4
  provider: ^6.1.2
  go_router: ^14.8.1
  flutter_svg: ^2.0.17
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
